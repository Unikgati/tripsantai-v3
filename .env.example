# Example environment variables for Web App Baru
# Do NOT commit real secrets. Replace values in your deployment or local .env (gitignored).

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Service role key MUST never be committed. Use only on server-side functions.
# SUPABASE_SERVICE_ROLE_KEY=

# Cloudinary (if used)
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
# CLOUDINARY_API_KEY=            # server-only
# CLOUDINARY_API_SECRET=         # server-only

# Analytics / Error tracking
VITE_SENTRY_DSN=

# App behavior
VITE_APP_ENV=development

# Optional: 3rd party keys for payments (server-side preferred)
# PAYMENT_PROVIDER_API_KEY=

# Notes:
# - Store actual secrets in your deployment provider's secret store (Vercel, Netlify, GitHub Actions secrets, etc.).
# - Never expose service_role keys to client bundles. Use serverless endpoints for admin-only operations.
